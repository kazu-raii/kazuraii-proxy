{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "Content-Type"
        }
      ]
    }
  ]
}
```

5. **Save** → **Close**

**FILE 4: .gitignore**
1. **Right-click** → **New** → **Text Document**
2. **Name it:** `.gitignore` (include the dot!)
3. **Open with Notepad**
4. **Copy and paste:**
```
node_modules/
.env
.vercel
.DS_Store
```

5. **Save** → **Close**

### **Your folder should now look like:**
```
📁 Desktop/roblox-proxy/
   📁 api/
      📄 proxy.js
   📄 package.json
   📄 vercel.json
   📄 .gitignore
```

---

## **STEP 3: Upload to GitHub** (3 minutes)

### **Website:** [github.com](https://github.com)

1. **Go to GitHub** → **Sign in**
2. **Click the "+" icon** (top right) → **"New repository"**
3. **Repository name:** `roblox-proxy`
4. **Keep it Public**
5. **DON'T check** any boxes
6. **Click "Create repository"**

**Now upload your files:**

7. **Click "uploading an existing file"** (blue link in the middle)
8. **Open File Explorer** → Go to your `Desktop/roblox-proxy` folder
9. **Select ALL 4 items** (api folder, package.json, vercel.json, .gitignore)
10. **Drag them** into the GitHub page
11. **Wait for upload** to complete
12. **Scroll down** → **Click "Commit changes"**

✅ **Your code is now on GitHub!**

---

## **STEP 4: Deploy to Vercel** (2 minutes)

### **Website:** [vercel.com](https://vercel.com)

1. **Open new tab** → Go to `vercel.com`
2. **Click "Start Deploying"** or **"Sign Up"**
3. **Click "Continue with GitHub"**
4. **Authorize Vercel** (click the button)
5. **Click "Import Project"** or **"Add New"** → **"Project"**
6. **Find your repository:** `roblox-proxy`
7. **Click "Import"**
8. **Don't change anything** → **Click "Deploy"**

⏳ **Wait 1-2 minutes** while Vercel deploys...

🎉 **Success!** You'll see: **"Congratulations!"**

---

## **STEP 5: Get Your Proxy URL** (1 minute)

1. **Copy the URL** Vercel shows you (something like: `https://roblox-proxy-xxxxx.vercel.app`)
2. **Click "Continue to Dashboard"**
3. **Your proxy is live!**

### **Your final proxy URL will be:**
```
https://roblox-proxy-xxxxx.vercel.app/api/proxy?url=